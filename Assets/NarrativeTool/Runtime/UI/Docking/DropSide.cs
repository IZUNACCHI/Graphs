namespace NarrativeTool.UI.Docking
{
    /// <summary>Where on a target <see cref="DockArea"/> a tab is being dropped.</summary>
    public enum DropSide
    {
        Center,   // merge into existing tab strip
        Left,
        Right,
        Top,
        Bottom,
    }
}
